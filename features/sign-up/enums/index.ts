export enum SignUpFormKeys {
  FIRST_NAME = "first_name",
  LAST_NAME = "last_name",
  EMAIL = "email",
  PHONE_NUMBER = "phone",
  PASSWORD = "password",
  CONFIRM_PASSWORD = "confirm_password",
}

export enum VerifyPhoneNumberKeys {
  CODE = "code",
}
