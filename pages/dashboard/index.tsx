import type { NextPage } from "next";
import * as Styled from "../index.styled";

const Home: NextPage = () => {
  return (
    <Styled.Container>
      <Styled.Main>
        <h1>Main page</h1>
      </Styled.Main>
    </Styled.Container>
  );
};

export default Home;
